server:
  port: 9999

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

eureka:
  client:
    service-url:
      defaultZone: http://serverreg:8761/eureka/