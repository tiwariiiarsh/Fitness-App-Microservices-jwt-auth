server:
  port: 9095

spring:
  application:
    name: USER-SERVICE

  config:
    import: optional:configserver:http://configserver:9999   # 🔥 localhost hatao

  cloud:
    config:
      fail-fast: false